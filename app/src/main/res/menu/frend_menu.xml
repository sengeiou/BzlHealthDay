<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <!--
    showAsAction主要是针对菜单的显示起作用的，它有三个可选项
    always：总是显示在界面上
    never：不显示在界面上，只让出现在右边的三个点中
    ifRoom：如果有位置才显示，不然就出现在右边的三个点中
    -->
    <item
        android:id="@+id/action_add_new_frend"
        android:orderInCategory="100"
        android:title="@string/string_add_frend"
        android:icon="@mipmap/ic_seach_add_frend"
        app:actionViewClass="android.support.v7.widget.SearchView"
        app:showAsAction="never" />
    <item
        android:icon="@mipmap/ic_frend_apply_add"
        android:id="@+id/action_new_frend_apply"
        android:orderInCategory="100"
        android:title="@string/string_frend_apply"
        app:showAsAction="never" />
</menu>